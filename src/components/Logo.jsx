import logo from "../assets/Netflix_Logo_PMS.png"

const Logo = () => {
    
    return (
        <div className="logo">
            <img src={logo} alt="Netflix written in red, with a little curve at the bottom" />
        </div>
    );
}

export default Logo;